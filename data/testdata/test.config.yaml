samples: "data/testdata/test.data.tsv"

### short read trimming
illumina_trimming: ["trimgalore"]

### short read error correction
illumina_correction: ["bless", "spades"]

### short read merging
illumina_merge:
   merger: ["usearch"] #, "flash"]
   merge_at_stage: ["trimmed", "corrected"] #could be "trimmed", "corrected" or both

#ont basecallers
ont_basecalling:
   basecaller: ["guppy", "flappie"] #"bonito"]
   concurrency: 50
   minlength: 1000
   guppy:
      kit: "SQK-LSK109" #"SQK-LSK110",
      flowcell: "FLO-MIN106" #"FLO-MIN111",
      optional_params: "--min_qscore 5" #--disable_qscore_filtering

#long read correction
long_correction: ["ratatosk", "canucorrect", "consent"] #"ratatosk", "consent", "canu"]

bless_k: [21,31,41,51,61,71,81]

kmc:
   max_mem_in_GB: 24
   k: [21] #,31,41,51,61,71]
   mincount: 2
   maxcount: 1000000000
   maxcounter: 255

### short read assembly
assemble: 
   assembler: ["abyss", "minia", "platanus", "spades", "haslr", "abyss_scaffold", "spades_hybrid", "flye", "canu"] 
   assemble_at_stage: ["trimmed", "corrected", "merged"] #possible are "trimmed", "corrected", "merged"
   default_k: [51]
#assembler: ["haslr"] #, "spades", "platanus"]

evaluate_assemblies:
   evaluator: ["quast", "busco", "blobtools"]
   minlength: 1000
   busco:
      set: "eukaryota_odb10"
      augustus_species: "schistosoma"
      additional_parameters: ""

#ectools:
#   ec_concurrency: 100
#   reads_per_file: 100
#   files_per_dir: 10
#   unitig_similarity: [0.98, 1.0]
#   minlength_in: 1000
#   minlength_out: 900

threads:
   samtools: 4
   trimmomatic: 24
   kmc: 24
   bless: 40
   spades_correct: 90
   mergepairs_usearch: 20
   kmergenie: 40
   abyss: 92
   minia: 90
   platanus: 90
   spades: 90
   quast: 4
   busco: 40
   bowtie2: 8
   samtools: 8
   samtools_index: 4
   guppy: 10
max_mem_in_GB:
   bless: 80
   abyss: 750
   platanus: 750
